# OpenCL Demonstration Program

The program adds a linear combination of two vectors

1. Install dependencies
```bash
sudo apt install ocl-icd-opencl-dev opencl-headers
```
2. Build as usual
```bash
mkdir build
cd build
cmake ..
make
```